version: '3'
services:
  web:
    build:
      context: .
      dockerfile: docker/web/Dockerfile
    image: personal-inventory
    ports:
      - 80:80
    volumes:
      - ./data/images:/data/images

  mongodb:
    image: mongo:4.0.0
    volumes:
      - ./data/db:/data/db
